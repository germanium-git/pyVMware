---
  # Specify credentials
  vsphere_ip: 1.2.3.4
  account: admin
  # Password is optional. Comment it and enter the password in input dialog.
  passw: my_password

  banner: You are doing some changes in Mylab

  # Specify the switch the distributed port groups will be created
  dswitch: switchname
  # Specify active uplink interfaces or comment it to inherit the implicit distributed switch's setting
  #actuplink:
  #    - Uplink 1
  #    - Uplink 2
